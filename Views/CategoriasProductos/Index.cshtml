@* @model IEnumerable<prueba2maya.Models.CategoriasProducto>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<!-- Mostrar solo si el usuario es administrador -->
@if (User.IsInRole("Administrador"))
{
    <p>
        <a asp-action="Create">Create New</a>
    </p>
}

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreCategoria)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Descripcion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.NombreCategoria)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Descripcion)
                </td>
                <td>
                    <!-- Mostrar los botones de acción solo si el usuario es administrador -->
                    @if (User.IsInRole("Administrador"))
                    {
                        <a asp-action="Edit" asp-route-id="@item.IdCategoria">Edit</a>
 
                    <a asp-action="Details" asp-route-id ="@item.IdCategoria"> Details</a> 
                    <a asp-action="Delete" asp-route-id="@item.IdCategoria"> Delete </a>
                    }
                </td>
            </tr>
        }
    </tbody>
</table>
 *@

@* @model IEnumerable<prueba2maya.Models.CategoriasProducto>

@{
    ViewData["Title"] = "Index";
}

<div class="container my-5">
    <div class="text-center mb-4">
        <h1 style="color: #B5651D; font-family: 'Georgia', serif;">Categorías de Productos</h1>
        <p style="color: #8B4513;">Aquí puedes gestionar las categorías de los productos.</p>
    </div>

    <!-- Mostrar solo si el usuario es administrador -->
    @if (User.IsInRole("Administrador"))
    {
        <div class="d-flex justify-content-end mb-3">
            <a asp-action="Create" class="btn btn-warning text-white" style="background-color: #FFBF00; border: none;">
                <i class="bi bi-plus-circle"></i> Crear Nueva Categoría
            </a>
        </div>
    }

    <table class="table table-striped table-hover" style="border: 1px solid #D2B48C;">
        <thead class="table-light" style="background-color: #FFF3CD;">
            <tr>
                <th>@Html.DisplayNameFor(model => model.NombreCategoria)</th>
                <th>@Html.DisplayNameFor(model => model.Descripcion)</th>
                <th class="text-center">Acciones</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@Html.DisplayFor(modelItem => item.NombreCategoria)</td>
                    <td>@Html.DisplayFor(modelItem => item.Descripcion)</td>
                    <td class="text-center">
                        <!-- Mostrar los botones de acción solo si el usuario es administrador -->
                        @if (User.IsInRole("Administrador"))
                        {
                            <a asp-action="Edit" asp-route-id="@item.IdCategoria" class="btn btn-sm btn-outline-warning">
                                <i class="bi bi-pencil-square"></i> Editar
                            </a>
                            <a asp-action="Details" asp-route-id="@item.IdCategoria" class="btn btn-sm btn-outline-info">
                                <i class="bi bi-info-circle"></i> Detalles
                            </a>
                            <a asp-action="Delete" asp-route-id="@item.IdCategoria" class="btn btn-sm btn-outline-danger">
                                <i class="bi bi-trash"></i> Eliminar
                            </a>
                        }
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

<style>
    body {
        background-color: #FFF8E7;
    }

    .btn-outline-warning {
        color: #B5651D;
        border-color: #B5651D;
    }

        .btn-outline-warning:hover {
            background-color: #B5651D;
            color: #FFF;
        }

    .btn-outline-info {
        color: #6C757D;
        border-color: #6C757D;
    }

        .btn-outline-info:hover {
            background-color: #6C757D;
            color: #FFF;
        }

    .btn-outline-danger {
        color: #8B0000;
        border-color: #8B0000;
    }

        .btn-outline-danger:hover {
            background-color: #8B0000;
            color: #FFF;
        }

    .table-light {
        background-color: #FFF3CD;
    }

    .table-hover tbody tr:hover {
        background-color: #F1E6B7;
    }

    .text-center {
        text-align: center;
    }
</style>
 *@


@model IEnumerable<prueba2maya.Models.CategoriasProducto>

@{
    ViewData["Title"] = "Index";
}

<div class="container my-5">
    <div class="text-center mb-4">
        <h1 class="titulo-categoria">Categorías de Productos</h1>
        <p class="descripcion-categoria">Aquí puedes gestionar las categorías de los productos.</p>
    </div>

    <!-- Mostrar solo si el usuario es administrador -->
    @if (User.IsInRole("Administrador"))
    {
        <div class="d-flex justify-content-end mb-3">
            <a asp-action="Create" class="btn btn-warning text-white btn-crear-categoria">
                <i class="bi bi-plus-circle"></i> Crear Nueva Categoría
            </a>
        </div>
    }

    <table class="table table-striped table-hover tabla-categoria">
        <thead class="table-light">
            <tr>
                <th>@Html.DisplayNameFor(model => model.NombreCategoria)</th>
                <th>@Html.DisplayNameFor(model => model.Descripcion)</th>
                <th class="text-center">Acciones</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@Html.DisplayFor(modelItem => item.NombreCategoria)</td>
                    <td>@Html.DisplayFor(modelItem => item.Descripcion)</td>
                    <td class="text-center">
                        <!-- Mostrar los botones de acción solo si el usuario es administrador -->
                        @if (User.IsInRole("Administrador"))
                        {
                            <a asp-action="Edit" asp-route-id="@item.IdCategoria" class="btn btn-sm btn-outline-warning">
                                <i class="bi bi-pencil-square"></i> Editar
                            </a>
                            <a asp-action="Details" asp-route-id="@item.IdCategoria" class="btn btn-sm btn-outline-info">
                                <i class="bi bi-info-circle"></i> Detalles
                            </a>
                            <a asp-action="Delete" asp-route-id="@item.IdCategoria" class="btn btn-sm btn-outline-danger">
                                <i class="bi bi-trash"></i> Eliminar
                            </a>
                        }
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
